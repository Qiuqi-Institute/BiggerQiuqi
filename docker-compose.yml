services:
  biggerqiuqi:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: biggerqiuqi
    ports:
      - "${APP_PORT}:80"
    restart: unless-stopped
    volumes:
      - ./runtime/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro